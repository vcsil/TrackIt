import { Bars } from  'react-loader-spinner'

function Loading() {
    <Bars
        height="100"
        width="100"
        color='white'
        ariaLabel='loading'
    />
};

export default Loading;
